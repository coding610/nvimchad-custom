# Prerequisites
Nvim-Chad neovim configuration with no custom configuration
(No folder named custom inside the lua folder)


# Installation
```
cd ∼/.config/nvim/lua
git clone https://github.com/coding610/nvimchad-custom/
mv nvimchad-custom custom
```
